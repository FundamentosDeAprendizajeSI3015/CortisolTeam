# Dataset descargado (ZIP y carpeta extraída)
data/extracted/
data/*.zip

# PDFs (except the final report)
*.pdf
!reports/model_report.pdf

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Kaggle credentials (nunca subir)
kaggle.json
